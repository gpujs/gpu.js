///
/// Class: CoreUtils
///
/// Minimilistic version of `utils.js` with reduced usage in GPUCore class
/// This is to provide non breaking migration for building GPUCore.
///
module.exports = class CoreUtils {
	
}